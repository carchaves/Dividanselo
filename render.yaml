services:
  - type: web
    name: dividanselo-backend
    runtime: node
    rootDir: server
    buildCommand: rm -rf node_modules && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
